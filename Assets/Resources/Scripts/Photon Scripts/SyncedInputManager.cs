using System.Collections;
using System.Collections.Generic;
using UnityEngine;

using Photon.Pun;
using Photon.Realtime;

public class SyncedInputManager : MonoBehaviourPun
{
    /**
    private GameObject[] syncedHand;

    public void ManageSyncedInputs(){
        // gets all direct interactor hands (the only type of hand being synced via PhotonView)
        hands = Object.FindObjectsOfType<>();
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    */
}
